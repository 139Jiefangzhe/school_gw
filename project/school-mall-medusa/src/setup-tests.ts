/**
 * Test setup file
 * Configures the testing environment for Medusa v2 integration tests
 */
import { initDb, dropDatabase } from "@medusajs/medusa/test-utils";

beforeAll(async () => {
  // Database initialization is handled by Medusa test utils
});

afterAll(async () => {
  // Cleanup after all tests
});
